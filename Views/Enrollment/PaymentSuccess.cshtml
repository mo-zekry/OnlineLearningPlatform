@{
    ViewData["Title"] = "Payment Success";
}

<div class="container my-5 vh-100">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-body">
                    <h2 class="text-center mb-4">Payment Success</h2>

                    <div class="alert alert-success text-center" role="alert">
                        <p class="mb-0">Your payment was successful. You are now enrolled in the course.</p>
                    </div>

                    <div class="d-flex justify-content-center mt-4">
                        <a asp-action="Index" class="btn btn-primary">Back to Enrollments</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
